# Public variables (accessible in browser)
# Use NEXT_PUBLIC_ prefix for any variables that need to be available in client-side code
NEXT_PUBLIC_API_URL=https://api.example.com

# Private variables (server-side only)
# These are only available in server components, API routes, and middleware
API_KEY=your_secret_api_key
